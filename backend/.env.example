# Azure Speech Services
AZURE_SPEECH_KEY=your_azure_speech_key_here
AZURE_SPEECH_REGION=your_region_here

# OpenAI API (for quiz generation)
OPENAI_API_KEY=your_openai_api_key_here

# Alternative: Anthropic Claude API (for quiz generation)
# ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Fuzzy matching threshold (0.0 to 1.0, lower = more lenient)
# Recommended for young readers: 0.40
WORD_MATCH_THRESHOLD=0.40

# Minimum buffered audio size (bytes) before processing
# ~12000 bytes ≈ ~375ms at 16kHz mono PCM
# Tune higher for better accuracy, lower for faster feedback
AUDIO_PROCESS_BYTES=12000
